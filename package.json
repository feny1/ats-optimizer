{
  "name": "ats-optimizer",
  "version": "1.0.0",
  "description": "Deterministic ATS resume tailorer on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.75.0"
  }
}

